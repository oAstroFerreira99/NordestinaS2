import styled from "styled-components";

export const WrapperPenalCode = styled.div`
  display: flex;
  flex-direction: column;
  gap: 16px;
  max-width: 100%;
  height: 100%;

  > iframe {
    border: 0;
  }
`;
